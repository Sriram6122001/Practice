void main()
{
  List <int>collections = [1,2,3,4,5];
  // print(collections.length);
  // print(collections.reversed);
  // print(collections.first);
  // print(collections.hashCode);
  // print(collections.runtimeType);
  // print(collections.isEmpty);
  // print(collections.isNotEmpty);
  // print(collections.iterator);
  print(collections.single);
  // print(collections.last);
}